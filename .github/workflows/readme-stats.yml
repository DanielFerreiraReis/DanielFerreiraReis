name: Generate GitHub Stats

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Generate stats card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: stats
          options: username=DanielFerreiraReis&show_icons=true&theme=github_dark&include_all_commits=true
          path: profile/stats.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate top languages card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: top-langs
          options: username=DanielFerreiraReis&layout=compact&langs_count=8&theme=github_dark
          path: profile/top-langs.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git add profile/*.svg
<<<<<<< HEAD
=======
          git commit -m "Update GitHub stats" || exit 0
          git push
          git config user.email "github-actions@users.noreply.github.com"
          git add stats.svg top-langs.svg
>>>>>>> 63aad827b80f2b7b8e41755cb13882c58935c8d1
          git commit -m "Update GitHub stats" || exit 0
          git push
